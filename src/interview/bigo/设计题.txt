设计思考：
1.设计一个缓存对象数据结构，如下：
（1）使用两个数据结构：队列Queue和Map:对于用户的每个请求，将其请求ID存入Queue中，同时将其ID和当前时间构成键值对存放如Map中；
（2）业务过期处理：对于业务响应，如果在5s时间内响应了，通过ID将Queue和Map的数据都删除掉；
（3）对于一个5s没有响应的数据的处理策略：使用一个单独线程对Queue进行遍历，同时在Map中（查找较快）获取其时间，如果超时了，则将发出必要的提示响应，并将其删除；否则，如果当前查找的Queue还没过期，则回到Queue队头，依次循环。（可以设置每次的扫描时间间隔）。