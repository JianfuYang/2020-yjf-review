1、模式定义：
    为请求创建了一个接收者对象的链。

2、示例场景：服务器后端在对前端发送过来的请求进行处理的时候，一般的一个简单的过滤认证行为为：
    Request—>请求频率验证—>登录认证—>访问权限—>敏感词过滤—>...
    我们可以直接在程序代码中进行顺序流程代码编写，但这样的代码耦合不易于扩展维护和复用，我们可以使用责任链的形式来实现，
    责任链模式主要可以采用链表的形式进行实现。


3、典型应用：Filter,FilterChain类